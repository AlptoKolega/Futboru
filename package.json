{
  "name": "futboru",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimalistyczny, źródłowy feed transferów piłkarskich.",
  "scripts": {
    "refresh": "node scripts/refresh.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm test && npm run refresh && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
